/**
 * Redux Action Types
 *
 * Available action types for Redux dispatch operations
 * for the application.
 */

// Language
const SET_LANGUAGE = "SET_LANGUAGE";

// User
const SET_USER = "SET_USER";
const LOGOUT_USER = "LOGOUT_USER";

// Eexport the action types.
export {
    SET_LANGUAGE,
    SET_USER,
    LOGOUT_USER
};